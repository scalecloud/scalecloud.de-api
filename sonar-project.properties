sonar.projectKey=scalecloud_scalecloud.de-api
sonar.organization=scalecloud

# This is the name and version displayed in the SonarQube Cloud UI.
#sonar.projectName=scalecloud.de-api
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

# Go test coverage report, generated by the "Test with coverage" step in the workflows.
# Adjust this path if your go.mod / module root is not at ./cmd/scalecloud.de-api.
sonar.go.coverage.reportPaths=cmd/scalecloud.de-api/coverage.out